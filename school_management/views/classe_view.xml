<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Classe View -->
        <record model="ir.ui.view" id="view_classe_form">
            <field name="name">classe.form</field>
            <field name="model">classe</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="class_name"/>
                            <field name="student_ids"/>
                            <field name="teacher_ids" widget="many2many_tags"/>
                            <field name="capacity"/>
                            <field name="room"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Classe Tree View -->
        <record model="ir.ui.view" id="view_classe_tree">
            <field name="name">classe.tree</field>
            <field name="model">classe</field>
            <field name="arch" type="xml">
                <list>
                    <field name="class_name"/>
                    <field name="student_count"/>
                    <field name="teacher_ids" widget="many2many_tags"/>
                    <field name="capacity"/>
                    <field name="room"/>
                </list>
            </field>
        </record>

        <record id="action_school_classe" model="ir.actions.act_window">
            <field name="name">Classes</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">classe</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Wizard View for Assigning Teacher to Class -->
        <record id="view_classe_assign_teacher_wizard" model="ir.ui.view">
            <field name="name">classe.assign.teacher.wizard.form</field>
            <field name="model">classe.assign.teacher.wizard</field>
            <field name="arch" type="xml">
                <form string="Assign Teacher to Class">
                    <sheet>
                        <group>
                            <field name="classe_id"/>
                            <field name="current_teacher_ids" invisible="not current_teacher_ids"
                                   widget="many2many_tags"/>
                            <field name="teacher_id"/>
                        </group>
                    </sheet>

                    <footer>
                        <button name="action_confirm_assignment"
                                string="Assign Teacher"
                                type="object"
                                class="btn-primary"/>
                        <button string="Cancel"
                                class="btn-secondary"
                                special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
        <!-- Search View for Classes -->
        <record id="view_classe_search" model="ir.ui.view">
            <field name="name">classe.search</field>
            <field name="model">classe</field>
            <field name="arch" type="xml">
                <search>
                    <field name="class_name"/>
                    <field name="room"/>
                    <filter string="My Classes" name="my_classes"
                            domain="[('teacher_ids.employee_id.user_id', '=', uid)]"/>
                    <group expand="0" string="Group By">
                        <filter string="Room" name="group_by_room" context="{'group_by': 'room'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- Update the action to use this search view -->
        <record id="action_school_classe" model="ir.actions.act_window">
            <field name="name">Classes</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">classe</field>
            <field name="view_mode">list,form</field>
            <field name="search_view_id" ref="view_classe_search"/>
            <field name="context">{'search_default_my_classes': 1}</field>
        </record>
        <!-- Menu Item -->
        <menuitem id="menu_classes" name="Classes" parent="main_menu" action="action_school_classe"/>
    </data>
</odoo>